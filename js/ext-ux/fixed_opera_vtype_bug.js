/*
 * File: miFacturaWeb.LoginForm.js
 * Date: Wed May 25 2011 10:24:49 GMT-0600 (Hora verano, Montañas (México))
 * 
 * This file was generated by Ext Designer version 1.1.2.
 * http://www.sencha.com/products/designer/
 *
 * This file will be generated the first time you export.
 *
 * You should implement event handling and custom methods in this
 * class.
 */
Ext.override(Ext.form.TextField, {
    filterKeys: function(e) {
        if (e.isSpecialKey()) {
            // special keys don't generate charCodes, so leave them alone
            return;
        }

        var c = e.getCharCode(),
            cc = String.fromCharCode(c);

        if (!this.maskRe.test(cc)) {
            e.stopEvent();
        }
    }
});

Ext.override(Ext.EventObjectImpl, {
    isSpecialKey: function() {
        var k = this.normalizeKey(this.keyCode);
        return (this.type == 'keypress' && this.ctrlKey) ||
                this.isNavKeyPress()  ||
                (k == this.BACKSPACE) || // Backspace
                (k >= 16 && k <= 20)  || // Shift, Ctrl, Alt, Pause, Caps Lock
                (k >= 44 && k <= 45);    // Print Screen, Insert
    }
});

if (Ext.version.match(/(\d+).*/)[1] < 3) {
    // 2.x Backwards compat override
    Ext.override(Ext.EventObjectImpl, {
        normalizeKey: function() {
            var safariKeys = {
                3 : 13,     // enter
                63234 : 37, // left
                63235 : 39, // right
                63232 : 38, // up
                63233 : 40, // down
                63276 : 33, // page up
                63277 : 34, // page down
                63272 : 46, // delete
                63273 : 36, // home
                63275 : 35  // end
            };
            
            return function(k) {
                return Ext.isSafari ? (safariKeys[k] || k) : k;
            }
        }()
    });
}